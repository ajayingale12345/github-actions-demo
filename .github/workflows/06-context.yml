name : 06-context
run_name : 06-context | DEBUG -${{inputs.debug}}
on :
   push :
   workflow_dispatch :
     inputs :
      debug :
       type : boolean
        default : false
job : 
 echo-data :
   runs-on : ubuntu-latest
   -steps:
      - name : Display Information
       run: |
       echo "Event Name : ${{github.event_name}}"
       echo "Ref:  ${{github.ref}}" 
       echo "SHA:  ${{github.sha}}" 
       echo "Actions ${{github.actions}}"
       echo "workflow {{github.activeworkflow}}"